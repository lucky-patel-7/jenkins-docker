pipeline {
    agent any
    tools {
        // Define the Docker tool type and version
        dockerTool 'Docker'
    }
  stages {
    stage('Test') {
     steps {
        sh 'node --version'
      }
      }
    }
  }

